@page
@model             IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AuditRouteWeld[0].Weld)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AuditRouteWeld[0].AuditRoute)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.AuditRouteWeld) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Weld.WeldID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AuditRoute.AuditRouteName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    <a asp-page="./Delete" asp-route-routeID="@item.AuditRouteId" asp-route-weldID="@item.WeldId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
